import SwiftUI

struct RecipesView: View {
    var body: some View {
        NavigationView {
            Text("🧂 Tu będą przepisy")
                .navigationTitle("Przepisy")
        }
    }
}
