
import SwiftUI

struct StatisticsView: View {
    var body: some View {
        NavigationView {
            Text("Statystyki")
                .font(.largeTitle)
                .navigationTitle("Statystyki")
            // TODO: Implement statistics views (e.g., most used ingredients, most cooked recipes)
        }
    }
}

struct StatisticsView_Previews: PreviewProvider {
    static var previews: some View {
        StatisticsView()
    }
}


