import SwiftUI

struct ShoppingListView: View {
    var body: some View {
        NavigationView {
            Text("🧂 Tu będzie lista zakupów")
                .navigationTitle("Zakupy")
        }
    }
}
